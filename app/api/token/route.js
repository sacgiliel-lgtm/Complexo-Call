import { AccessToken, RoomServiceClient } from 'livekit-server-sdk';
import { createClient } from '@supabase/supabase-js';
import { verifyGuestTicket, getGuestTicketFromCookie } from '../../../lib/guestTicket';
import { getSupabaseAdmin } from '../../../lib/supabaseAdmin';
import crypto from 'crypto';

async function rateLimit(admin,key,max=10,seconds=60){const hashed=crypto.createHash('sha256').update(key).digest('hex');const {data,error}=await admin.rpc('consume_rate_limit',{p_key:hashed,p_max:max,p_window_seconds:seconds});if(error){console.error('Rate limit RPC:',error);return false;}return !!data?.[0]?.allowed;}
async function audit(user,room,role,ip,ua,blocked=false){const url=process.env.DISCORD_WEBHOOK_URL;if(!url)return;try{await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({embeds:[{title:blocked?'🚫 ACESSO BLOQUEADO • CPX':'🛡️ ACESSO À CALL • CPX',color:blocked?14431557:role==='admin'?15204456:role==='convidado'?16753920:10158312,fields:[{name:'Usuário',value:`\`${String(user).slice(0,64)}\``,inline:true},{name:'Cargo',value:`**${role}**`,inline:true},{name:'Canal',value:`\`# ${String(room).slice(0,80)}\``},{name:'IP',value:`||${String(ip||'não capturado').slice(0,80)}||`,inline:true},{name:'User-Agent',value:`\`\`\`${String(ua||'N/A').slice(0,180)}\`\`\``}],timestamp:new Date().toISOString()}]})});}catch(error){console.error('Discord audit:',error);}}
async function participantCount(room){try{const host=process.env.NEXT_PUBLIC_LIVEKIT_URL?.replace(/^wss:/,'https:').replace(/^ws:/,'http:');if(!host)return null;const service=new RoomServiceClient(host,process.env.LIVEKIT_API_KEY,process.env.LIVEKIT_API_SECRET);return (await service.listParticipants(room))?.length??0;}catch(error){console.error('LiveKit participant count:',error);return null;}}
export async function GET(request){const{searchParams}=new URL(request.url);const room=searchParams.get('room');const ip=request.headers.get('x-forwarded-for')?.split(',')[0]?.trim()||request.headers.get('x-real-ip')||'unknown';const ua=request.headers.get('user-agent')||'';if(!room)return Response.json({error:'Faltando room.'},{status:400});try{const admin=getSupabaseAdmin();if(!(await rateLimit(admin,`token:${ip}`)))return Response.json({error:'Muitas tentativas. Aguarde um instante.'},{status:429});const{data:channel,error:channelError}=await admin.from('channels').select('id,name,is_active,guest_access').eq('name',room).maybeSingle();if(channelError)throw channelError;if(!channel||!channel.is_active)return Response.json({error:'Sala inexistente ou desativada.'},{status:404});const{data:settings}=await admin.from('server_settings').select('maintenance_mode,max_users,discord_logs').eq('id',1).maybeSingle();const auth=request.headers.get('authorization');const guestTicket=getGuestTicketFromCookie(request)||request.headers.get('x-guest-ticket');let username,role,identity;if(auth?.startsWith('Bearer ')){const token=auth.slice(7);const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,{auth:{persistSession:false}});const{data:{user},error}=await supabase.auth.getUser(token);if(error||!user)return Response.json({error:'Sessão inválida ou expirada.'},{status:401});const{data:profile,error:profileError}=await admin.from('profiles').select('username,role,status').eq('id',user.id).single();if(profileError||!profile)return Response.json({error:'Perfil não encontrado.'},{status:403});if(profile.status==='suspenso')return Response.json({error:'Sua conta está suspensa.'},{status:403});username=profile.username||user.email?.split('@')[0]||'Membro';role=profile.role||'membro';identity=user.id;}else if(guestTicket){const payload=verifyGuestTicket(guestTicket);if(!payload)return Response.json({error:'Convite expirado ou inválido.'},{status:401});const{data:guest,error:guestError}=await admin.from('guest_sessions').select('jti,invite_id,username,expires_at,revoked_at').eq('jti',payload.jti).maybeSingle();if(guestError||!guest||guest.revoked_at||String(guest.invite_id)!==String(payload.inviteId)||new Date(guest.expires_at).getTime()<=Date.now())return Response.json({error:'Sessão de convidado expirada ou revogada.'},{status:401});const{data:invite,error:inviteError}=await admin.from('invites').select('room_name').eq('id',guest.invite_id).maybeSingle();if(inviteError||!invite)return Response.json({error:'Convite do convidado não encontrado.'},{status:401});username=guest.username;role='convidado';identity=`guest:${guest.jti}`;if(invite.room_name&&invite.room_name!==room){if(settings?.discord_logs!==false)await audit(username,room,role,ip,ua,true);return Response.json({error:'Este convite dá acesso somente à call para a qual você foi convidado.'},{status:403});}}else return Response.json({error:'Não autenticado. Faça login ou use um convite.'},{status:401});if(settings?.maintenance_mode&&role!=='admin'){if(settings?.discord_logs!==false)await audit(username,room,role,ip,ua,true);return Response.json({error:'O servidor está em manutenção no momento.'},{status:503});}if(settings?.max_users){const count=await participantCount(room);if(count!==null&&count>=settings.max_users){if(settings?.discord_logs!==false)await audit(username,room,role,ip,ua,true);return Response.json({error:'O limite de usuários desta call foi atingido.'},{status:429});}}const at=new AccessToken(process.env.LIVEKIT_API_KEY,process.env.LIVEKIT_API_SECRET,{identity,name:username,ttl:'10m',metadata:JSON.stringify({username,role})});at.addGrant({roomJoin:true,room,canPublish:true,canSubscribe:true});const token=await at.toJwt();if(settings?.discord_logs!==false)await audit(username,room,role,ip,ua,false);return Response.json({token,username,role});}catch(error){console.error('Token API:',error);return Response.json({error:'Erro interno ao processar acesso.'},{status:500});}}
